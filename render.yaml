services:
  - type: web
    name: loopthru-evidence-engine
    runtime: python
    plan: free
    buildCommand: "pip install -e ."
    startCommand: "uvicorn evidence_engine.api:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
